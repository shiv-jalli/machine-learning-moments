# Datasets

Use this folder only for small public or synthetic datasets.

Do not commit private, confidential, personal or large datasets.

Recommended structure:

```text
datasets/
├── sample/
├── external/
└── README.md
```

For larger datasets, link to the source and provide download instructions instead of storing the files in Git.
